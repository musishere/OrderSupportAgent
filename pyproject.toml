[project]
name = "order-support-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "musishere", email = "mustafabukhari333@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "huggingface-hub>=1.30.0",
    "langgraph>=1.2.11",
]

[project.scripts]
order-support-agent = "order_support_agent:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
